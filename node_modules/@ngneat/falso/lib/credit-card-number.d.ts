import { FakeOptions } from './core/core';
declare type Issuer = 'American Express' | 'UnionPay' | 'Diners Club' | 'Discover Card' | 'RuPay' | 'JCB' | 'Maestro' | 'Dankort' | 'Mastercard' | 'Visa' | 'Visa Electron' | 'UATP';
/**
 * Generate a random credit card number.
 *
 * @category Finance
 *
 * @example
 *
 * randCreditCardNumber()
 *
 * @example
 *
 * randCreditCardNumber({ issuer: 'Visa'})
 *
 * @example
 *
 * randCreditCardNumber({ length: 10 })
 *
 */
export declare function randCreditCardNumber<Options extends FakeOptions & {
    issuer?: Issuer;
} = never>(options?: Options): [Options] extends [never] ? unknown : Options["length"] extends number ? unknown[] : unknown;
export {};
