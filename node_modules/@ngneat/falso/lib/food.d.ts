import { FakeOptions } from './core/core';
export interface FoodOptions extends FakeOptions {
    origin?: 'china' | 'italy' | 'india' | 'mexico' | 'japan' | 'france' | 'lebanon' | 'thailand' | 'greece' | 'turkey';
}
/**
 * Generate a random food name.
 *
 * @category Food
 *
 * @example
 *
 * randFood()
 *
 * @example
 *
 * randFood({ origin: 'italy' })
 *
 * @example
 *
 * randFood({ length: 10 })
 *
 */
export declare function randFood<Options extends FoodOptions = never>(options?: Options): [Options] extends [never] ? string : Options["length"] extends number ? string[] : string;
