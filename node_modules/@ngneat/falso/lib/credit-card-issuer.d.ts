import { FakeOptions } from './core/core';
/**
 * Generate a random credit card issuer.
 *
 * @category Finance
 *
 * @example
 *
 * randCreditCardIssuer()
 *
 * @example
 *
 * randCreditCardIssuer({ length: 10 })
 *
 */
export declare function randCreditCardIssuer<Options extends FakeOptions = never>(options?: Options): [Options] extends [never] ? string : Options["length"] extends number ? string[] : string;
