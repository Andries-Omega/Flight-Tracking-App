import { FakeOptions } from './core/core';
interface BetweenOptions extends FakeOptions {
    from: Date;
    to: Date;
}
/**
 * Generate a random date between ranges.
 *
 * @category date
 *
 * @example
 *
 * randBetweenDate({ from: Date, to: Date })
 *
 * @example
 *
 * randBetweenDate({ from: Date, to: Date, length: 10 })
 *
 */
export declare function randBetweenDate<Options extends BetweenOptions = never>(options: Options): [Options] extends [never] ? Date : Options["length"] extends number ? Date[] : Date;
export {};
