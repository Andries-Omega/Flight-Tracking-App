import { FakeOptions } from './core/core';
/**
 * Generate a random random uuid.
 *
 * @category general
 *
 * @example
 *
 * randUuid()
 *
 * @example
 *
 * randUuid({ length: 10 })
 *
 */
export declare function randUuid<Options extends FakeOptions = never>(options?: Options): [Options] extends [never] ? string : Options["length"] extends number ? string[] : string;
