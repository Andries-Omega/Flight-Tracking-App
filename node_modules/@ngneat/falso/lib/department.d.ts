import { FakeOptions } from './core/core';
/**
 * Generate a random department.
 *
 * @category Commerce
 *
 * @example
 *
 * randDepartment()
 *
 * @example
 *
 * randDepartment({ length: 10 })
 *
 */
export declare function randDepartment<Options extends FakeOptions = never>(options?: Options): [Options] extends [never] ? string : Options["length"] extends number ? string[] : string;
