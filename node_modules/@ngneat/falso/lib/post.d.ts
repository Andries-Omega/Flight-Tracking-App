import { FakeOptions } from './core/core';
import { User } from './user';
interface Post {
    id: string;
    title: string;
    body: string;
    comments: Array<{
        id: string;
        text: string;
        user: User;
    }>;
}
/**
 * Generate a random post.
 *
 * @category entities
 *
 * @example
 *
 * randPost()
 *
 * @example
 *
 * randPost({ length: 10 })
 *
 */
export declare function randPost<Options extends FakeOptions = never>(options?: Options): [Options] extends [never] ? Post : Options["length"] extends number ? Post[] : Post;
export {};
